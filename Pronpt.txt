①   ###要件
    テトロミの横移動の制限をチェックする関数を作成する
    テトロミノは回転するため、shapeの行列の右端は、変化する

    ###使用する関数名
    isValidPosition()

    ###要求
    - 両端のチェックは以下のようにすること
    左端チェック：tetromino.x + col >= 0
    右端チェック：tetromino.x + col < FIELD_WIDTH
    - moveTetromino()で作成した関数の条件を指定してテトロミノの動作に対して両端に制限を設ける
    - 既存のグローバル変数は変更しない

②   テトロミノの当たり判定は、枠によるものと積み重なったテトロミノ同士の当たり判定があるが、二つの判定に関する関数を追加した方が良いか？
    それとも一度しか呼び出さない場合、同じところに書いた方が良いか？メリットとデメリットの両方からアドバイスが欲しい

    結論：フィールド配列を用意し、使用する

③   ###要件
    テトロミのが最下部に達した時の挙動を示す関数を作成する
    テトロミノは最下部に達した時、ロックがかかり、動かなくなる。
    動かなくなると次のテトリスが出てくる

    ###使用する関数名
     -isTetrominoLock()

    ### 変更を加える関数
     - テトロミノを出す関数

    ###要求
    - 既存のグローバル変数は変更しない

④   テトロミノが積まれていく時、色を保存すると処理の差で前の色が引き継がれてしまうのだが何か対応策はないか

⑤   ###要件
    テトロミノに新しく回転処理を追加する
    イメージはできているが、下書きが欲しい

    ### 作成する関数・名
    -  rotateTetromino()
    - rotateMatrix()

    ### 影響する関数
    - handleMovementKey()

　  ### 要求
    - キーは、
    "M"：右回転
    "N"：左回転

    - 既存のグローバル変数は変更しないこと

⑥   ### 要件
    てとろみの上端がフィールドの最上部に当たって時にロックとゲームオーバーの表示を追加する
    ###　
    関数名はこちらで命名するのでとりあえず出関数名を決めること

    ###
    gameオーバーのフラグを追加して、そのフラグによってゲームオーバーの判定を行う
    それ以外は私のほうで行う。

⑦   テトリスについて、実際のテトリスのシステムを参考にして配列が埋まり次第、その行を削除し上の配列を下におろしてくるようにしている。
    しかしなぜか、行の削除に時差を感じるが原因がわからないコンソールログを追加して原因を突き止めたい。

    原因：ハードドロップに行を削除する関数を入れていないため、自分の操作で行が動かなかった

⑧   Readmeを作成してほしい。ディレクトリ構造、着手手順書を